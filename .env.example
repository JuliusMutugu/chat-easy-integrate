# Environment Variables for Simple Messaging Platform

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (SQLite)
# For local development, this can be a relative path
# For production (Render), this should be an absolute path to persistent storage
DATABASE_URL=./messaging.db

# Client Configuration
# This should be the base URL where your application is served from
# For local development: http://localhost:3000
# For production: your Render service URL (e.g., https://chat-easy-integrate.onrender.com)
CLIENT_URL=http://localhost:3000
SERVER_URL=http://localhost:3000

# Production Example (uncomment for production):
# CLIENT_URL=https://chat-easy-integrate.onrender.com
# SERVER_URL=https://chat-easy-integrate.onrender.com

# CORS Configuration
# For production, you might want to restrict this to your domain
CORS_ORIGIN=*

# Socket.IO Configuration
# Additional socket configuration if needed
SOCKET_IO_CORS_ORIGIN=*
